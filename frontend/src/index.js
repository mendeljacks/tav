import React from 'react';
import ReactDOM from 'react-dom';
import {Login} from './login_visuals';

ReactDOM.render(
  <React.StrictMode>
    <Login />
  </React.StrictMode>,
  document.getElementById('root')
);